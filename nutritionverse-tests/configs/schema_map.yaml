# This file will be auto-generated by schema discovery
# Example structure (adjust after running --inspect):

dataset_format: nutritionverse_real

# Paths in the source annotations
image_path: image_path
foods_path: ingredients
dish_id_field: dish_id

# Per-food fields
food_fields:
  name: name
  mass_g: mass_g
  calories_kcal: calories_kcal
  protein_g: protein_g
  carbs_g: carbs_g
  fat_g: fat_g

# Total/aggregate fields
totals_fields:
  mass_g: totals.mass_g
  calories_kcal: totals.calories_kcal
  protein_g: totals.protein_g
  carbs_g: totals.carbs_g
  fat_g: totals.fat_g

# Optional fields
optional_fields:
  segmentation_mask: mask_path
  category: category
